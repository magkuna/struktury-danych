/*const fruit = [ "apple", "pear", "mango"];
fruit.forEach((element,index,array) => { 
    console.log(index)
})

const fruit = [ "apple", "pear", "mango"];
fruit.forEach((element,index,array) => { 
    console.log(element)
});*/

const fruit = [ "apple", "pear", "mango"];
fruit.forEach((element,index,array) => { 
    console.log(array);
    console.log(index);
    console.log(element);
});

